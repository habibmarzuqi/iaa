{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "node scripts/vercel-prebuild.mjs && prisma generate && prisma db push --accept-data-loss --skip-generate && next build",
  "installCommand": "npm install && npm install sharp",
  "functions": {
    "src/app/api/**/route.ts": {
      "maxDuration": 30
    }
  }
}
