{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "node scripts/vercel-prebuild.mjs && rm -rf node_modules/.prisma && prisma generate && next build",
  "installCommand": "npm install && npm install sharp",
  "functions": {
    "src/app/api/**/route.ts": {
      "maxDuration": 30
    }
  }
}
