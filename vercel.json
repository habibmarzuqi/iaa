{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "node scripts/vercel-prebuild.mjs && prisma generate && next build",
  "installCommand": "rm -rf node_modules/.prisma && npm install && npm install sharp",
  "functions": {
    "src/app/api/**/route.ts": {
      "maxDuration": 30
    }
  }
}
